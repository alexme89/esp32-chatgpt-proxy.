services:
  - type: web
    name: esp32-chatgpt-proxy
    env: python
    plan: free
    region: oregon
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn proxy:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: HUGGINGFACE_API_KEY
        value: "TU_HUGGINGFACE_API_KEY_AQUI"
