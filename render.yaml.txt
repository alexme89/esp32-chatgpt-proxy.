services:
  - type: web
    name: esp32-hf-proxy
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn proxy_hf:app --host 0.0.0.0 --port $PORT"
