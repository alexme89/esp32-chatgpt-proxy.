services:
  - type: web
    name: esp32-chatgpt-proxy
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn proxy:app --host 0.0.0.0 --port 10000
    plan: free
